title: dwellMode
type: object
properties:
  eventMode:
    type: string
    default: dwell
    description: The event processing mode of the reader.
    enum:
      - dwell
    example: dwell
  start:
    $ref: ./readinterval.v1.yaml
  stop:
    $ref: ./readinterval.v1.yaml
required:
  - eventMode
  - start
  - stop
