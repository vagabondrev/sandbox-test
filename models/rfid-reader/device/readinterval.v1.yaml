title: readInterval
type: object
properties:
  interval:
    type: integer
    example: '10'
    description: The numeric value for the interval.
  unit:
    type: string
    enum:
      - reads
      - seconds
      - minutes
      - hours
      - days
    example: seconds
    description: The unit of the interval.
